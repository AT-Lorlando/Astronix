auto-install-peers=true
strict-peer-dependencies=false
# Hoist ESLint stack so @nuxt/eslint's flat config can resolve its parsers
# (vue-eslint-parser, @typescript-eslint/parser) under pnpm's strict node_modules.
public-hoist-pattern[]=*eslint*
